// let task = await GroupTask.findOne({"tasks._id": req.body._id});
// let indexNumber;
// let index = task.tasks.map((current,index) => req.body._id == current._id? indexNumber = index : "");

//     delete task.tasks[indexNumber]
//  let newtask = task.tasks
//  console.log(newtask)
//  console.log(3,task)
//  task.tasks = task.save().then(console.log("saved"))